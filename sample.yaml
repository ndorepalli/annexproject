---
 - hosts: all
   tasks
   - name: install httpd
     yum: name=httpd state=installed 

   - name: start httpd
     service: name=httpd state=started

   - name: create diretory
     file: name=/tmp/king state=touch